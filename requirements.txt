# Core dependencies (required)
transformers>=4.30.0
datasets>=2.12.0
torch>=2.0.0
scikit-learn>=1.3.0
xgboost>=2.0.0
gradio>=3.40.0
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0
plotly>=5.14.0
flask>=3.0.0
flask-cors>=4.0.0

# Optional: concrete-ml (for real FHE - Linux/WSL/Colab only)
# On Windows, the project uses an FHE simulator instead
# Uncomment the line below if you're on Linux/WSL or Google Colab:
# concrete-ml>=1.0.0

# Optional: FastAPI (not used in current version, but kept for compatibility)
# fastapi>=0.100.0
# uvicorn>=0.23.0

